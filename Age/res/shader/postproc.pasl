out vec4 FragColor;

in vec2 uv;
uniform sampler2D tex;
uniform float exposure = 1.0;

void main()
{
    vec3 inColor = texture(tex, uv).rgb;
    vec3 mappedColor = learnopenglTonemap(inColor * exposure);
    FragColor = vec4(linearToScreen(mappedColor), 1.0f);
}
